#!/bin/bash

# This script is used to stop your Validator Client service.
# It is run in Native Mode only when the Smartnode needs to stop your Validator Client because of a misconfiguration or other error.
# It is not used in Docker Mode or Hybrid Mode.

# The command below is an example only.
# Replace it with your own command to stop your Validator Client service.

#sudo systemctl stop lighthouse-validator
